import {Dimensions} from 'react-native';
const {width, height} = Dimensions.get('window');

export const COLORS = {
  white: '#FFFFFF',
  black: '#000000',
};

export const SIZES = {
  base: 8,
  baseX2: 12,
  baseX3: 14,
  baseX4: 16,
  baseX5: 24,
  radius: 6,
};
